# cut, awk

## 1. cut

파일의 각 행에서 원하는 부분이나 필드만 잘라서 출력할 때 사용하는 명령어이다.

기본 형식:

```bash
cut OPTION... [FILE]...
```

---

## 2. cut 주요 옵션

### `-b`

바이트 단위로 출력한다.

```bash
cut -b
```

- 한글은 바이트 단위로 자르면 깨질 수 있음

### `-c`

문자 단위로 출력한다.

```bash
cut -c
```

### `-d`

필드 구분자를 지정한다.

```bash
cut -d
```

- 기본 필드 구분자인 탭 대신 다른 구분자를 사용할 때 사용

### `-f`

지정한 필드를 출력한다.

```bash
cut -f
```

예시:

```bash
cut -d ':' -f 1 /etc/passwd
```

- `:`을 구분자로 사용
- 첫 번째 필드만 출력

---

## 3. cut 범위 지정

### `N`

```text
N
```

- N번째 항목

### `N-`

```text
N-
```

- N번째부터 마지막까지

### `N-M`

```text
N-M
```

- N번째부터 M번째까지

### `-M`

```text
-M
```

- 첫 번째부터 M번째까지

---

## 4. awk

텍스트를 필드 단위로 처리하고 원하는 값을 추출할 때 사용하는 명령어이다.

기본 형식:

```bash
awk '동작' 파일
```

---

## 5. awk 필드 출력

`awk`에서는 한 행을 여러 필드로 나누어 사용할 수 있다.

```bash
awk '{print $1}' file
```

- 첫 번째 필드 출력

```bash
awk '{print $2}' file
```

- 두 번째 필드 출력

```bash
awk '{print $1, $2}' file
```

- 첫 번째와 두 번째 필드를 함께 출력

### `$0`

```bash
awk '{print $0}' file
```

- 현재 행 전체 출력

### `$NF`

```bash
awk '{print $NF}' file
```

- 마지막 필드 출력

---

## 6. awk 필드 구분자 지정

기본적으로 공백을 기준으로 필드를 구분한다.

다른 구분자를 사용하려면 `-F` 옵션을 사용한다.

```bash
awk -F ':' '{print $1}' /etc/passwd
```

- `:`을 필드 구분자로 사용
- 첫 번째 필드 출력

---

## 7. cut과 awk 비교

```text
cut : 일정한 구분자나 위치를 기준으로 원하는 부분을 간단하게 추출
awk : 필드 단위로 데이터를 처리하고 원하는 필드를 유연하게 출력
```

예시:

```bash
cut -d ':' -f 1 /etc/passwd
```

```bash
awk -F ':' '{print $1}' /etc/passwd
```

두 명령어 모두 첫 번째 필드를 추출할 수 있다.

---

## 8. 오늘 학습한 핵심 내용

```text
cut
-b : 바이트 단위
-c : 문자 단위
-d : 필드 구분자 지정
-f : 지정한 필드 출력

N   : N번째
N-  : N번째부터 마지막까지
N-M : N번째부터 M번째까지
-M  : 첫 번째부터 M번째까지

awk
$0  : 현재 행 전체
$1  : 첫 번째 필드
$2  : 두 번째 필드
$NF : 마지막 필드
-F  : 필드 구분자 지정
```
