# sort, shuf, uniq

## 1. sort

데이터를 원하는 기준으로 정렬할 때 사용하는 명령어이다.

```bash
sort
```

---

## 2. shuf

오늘 `shuf` 명령어를 학습했다.

```bash
shuf
```

---

## 3. uniq

오늘 `uniq` 명령어를 학습했다.

```bash
uniq
```

---

## 4. 오늘 학습한 핵심 명령어

```text
sort
shuf
uniq
```
